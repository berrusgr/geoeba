const {chromium}=require('C:/Users/HP/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert');
(async()=>{const b=await chromium.launch({headless:true,channel:'msedge'});try{const p=await b.newPage({viewport:{width:1440,height:1000}});await p.goto('http://localhost:3005');await p.getByRole('button',{name:'Serbest Stüdyo',exact:true}).click();await p.getByRole('button',{name:'Yazarak komut ver'}).click();const input=p.getByRole('combobox',{name:'Çizim komutu'});await input.fill('3 4 5 üçgen çiz');await input.press('Enter');await p.waitForFunction(()=>document.querySelector('[aria-label="Çizim komutu"]').value==='');const scene=()=>p.evaluate(()=>JSON.parse(localStorage.getItem('matematik_calisma_alani_v1')).objects);const before=await scene();const polygon=before.find(o=>o.type==='polygon');const open=()=>p.locator('[data-object-id="'+polygon.id+'"]').dispatchEvent('contextmenu',{clientX:850,clientY:460,button:2});await open();assert(await p.getByRole('menuitem',{name:'Yapıştır',exact:true}).isDisabled());await p.getByRole('menuitem',{name:'Kopyala',exact:true}).click();await p.locator('svg.w-full.h-full.block').dispatchEvent('contextmenu',{clientX:1150,clientY:550,button:2});await p.getByRole('menuitem',{name:'Yapıştır',exact:true}).click();assert.equal((await scene()).length,before.length*2);await p.getByTitle('Geri Al (Ctrl+Z)',{exact:true}).first().click();assert.equal((await scene()).length,before.length);await p.getByRole('button',{name:'Çember',exact:true}).click();await open();await p.getByRole('menuitem',{name:'Taşı (V)',exact:true}).click();assert.equal(await p.getByRole('button',{name:'Seç ve Taşı',exact:true}).getAttribute('aria-pressed'),'true');await open();await p.screenshot({path:'artifacts/context-clipboard.png'});console.log('PASS copy polygon, paste on blank canvas, undo as one action, Move activates V tool');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
